local p = require("fullerene.palette")

return {
    NoiceCmdlinePopup = { bg = p.bg_dark },
    NoiceCmdlinePopupBorder = { fg = p.blue, italic = false },
    NoiceCmdlinePopupTitle = { fg = p.red, italic = false },
    NoiceCmdlineIcon = { fg = p.blue },
}
